# 🚀 دليل النشر على Render.com

## الخطوات السريعة للنشر

### 1️⃣ إنشاء حساب على Render

1. اذهب إلى [render.com](https://render.com)
2. سجل حساب جديد (يمكنك استخدام حساب GitHub)
3. قم بتأكيد البريد الإلكتروني

### 2️⃣ ربط GitHub مع Render

1. من لوحة التحكم في Render، اضغط "New +"
2. اختر "Web Service"
3. اضغط "Connect GitHub"
4. اختر المستودع: `telegram-card-sender`
5. اضغط "Connect"

### 3️⃣ إعداد الخدمة

Render سيكتشف ملف `render.yaml` تلقائياً، لكن تأكد من الإعدادات:

- **Name**: telegram-card-sender
- **Environment**: Python
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python main_bot.py`
- **Plan**: Free

### 4️⃣ إضافة متغيرات البيئة

في صفحة الإعدادات، أضف المتغيرات التالية:

| المتغير | القيمة | الوصف |
|---------|--------|-------|
| `BOT_TOKEN` | توكن البوت من BotFather | مطلوب |
| `OWNER_ID` | معرف التيليجرام الخاص بك | مطلوب |
| `DB_ENCRYPTION_KEY` | (اتركه فارغاً) | سيتم توليده تلقائياً |

#### كيفية الحصول على المتغيرات:

**BOT_TOKEN:**
1. افتح [@BotFather](https://t.me/BotFather)
2. أرسل `/newbot`
3. اتبع التعليمات
4. احفظ التوكن

**OWNER_ID:**
1. افتح [@userinfobot](https://t.me/userinfobot)
2. سيرسل لك معرفك
3. احفظ الرقم

### 5️⃣ النشر

1. اضغط "Create Web Service"
2. انتظر حتى ينتهي البناء (Build)
3. البوت سيبدأ العمل تلقائياً!

### 6️⃣ التحقق من عمل البوت

1. افتح البوت في تيليجرام
2. أرسل `/start`
3. إذا رد عليك، البوت يعمل! ✅

---

## ⚠️ ملاحظات مهمة

### الخطة المجانية

Render يوفر خطة مجانية لكن:
- البوت سيتوقف بعد 15 دقيقة من عدم النشاط
- سيستيقظ تلقائياً عند إرسال رسالة
- 750 ساعة مجانية شهرياً

### الترقية للخطة المدفوعة

إذا أردت البوت يعمل 24/7:
1. اذهب إلى إعدادات الخدمة
2. اختر "Upgrade to Starter" ($7/شهر)
3. البوت سيعمل بدون توقف

### قاعدة البيانات

- قاعدة البيانات محفوظة في `/opt/render/project/src/`
- ستبقى البيانات حتى لو أعدت تشغيل البوت
- لكن قد تُحذف عند إعادة النشر

**للحفاظ على البيانات:**
1. استخدم Render Disk (مدفوع)
2. أو استخدم قاعدة بيانات خارجية (PostgreSQL)

---

## 🔧 التحديثات

عند تحديث الكود:

```bash
# في جهازك المحلي
git add .
git commit -m "Update: description of changes"
git push origin main
```

Render سيكتشف التحديث ويعيد النشر تلقائياً!

---

## 📊 مراقبة البوت

### السجلات (Logs)

1. اذهب إلى لوحة تحكم Render
2. اختر خدمة البوت
3. اضغط "Logs"
4. ستشاهد جميع الأحداث في الوقت الفعلي

### الإحصائيات

- استخدام الذاكرة
- استخدام المعالج
- وقت التشغيل
- عدد الطلبات

---

## 🐛 استكشاف الأخطاء

### البوت لا يرد

**الحل:**
1. تحقق من السجلات في Render
2. تأكد من صحة `BOT_TOKEN`
3. تأكد من صحة `OWNER_ID`

### خطأ في البناء (Build Failed)

**الحل:**
1. تحقق من ملف `requirements.txt`
2. تأكد من إصدار Python (3.11)
3. راجع السجلات للتفاصيل

### البوت يتوقف بعد فترة

**السبب:** الخطة المجانية
**الحل:** ترقية للخطة المدفوعة أو استخدام خدمة أخرى

---

## 🔄 بدائل Render

إذا أردت خيارات أخرى:

### 1. Railway.app
- مشابه لـ Render
- خطة مجانية محدودة
- سهل الاستخدام

### 2. Heroku
- خطة مجانية (محدودة)
- موثوق جداً
- يحتاج إضافة Procfile

### 3. PythonAnywhere
- خطة مجانية جيدة
- مناسب للبوتات الصغيرة
- واجهة سهلة

### 4. VPS (خادم خاص)
- تحكم كامل
- يعمل 24/7
- يحتاج خبرة تقنية
- أمثلة: DigitalOcean, Linode, Vultr

---

## 📝 ملف Procfile (للخدمات الأخرى)

إذا استخدمت Heroku أو خدمة تحتاج Procfile:

```
worker: python main_bot.py
```

---

## ✅ قائمة التحقق

قبل النشر، تأكد من:

- [ ] رفع الكود على GitHub
- [ ] إضافة `BOT_TOKEN` في متغيرات البيئة
- [ ] إضافة `OWNER_ID` في متغيرات البيئة
- [ ] ملف `render.yaml` موجود
- [ ] ملف `requirements.txt` محدث
- [ ] ملف `.gitignore` يحمي الملفات الحساسة

---

## 🎉 النشر الناجح!

إذا اتبعت الخطوات بشكل صحيح، البوت الآن يعمل على Render! 🚀

يمكنك:
- ✅ إضافة جلسات
- ✅ إنشاء مهام
- ✅ تشغيل المهام
- ✅ مراقبة الإحصائيات

**استمتع بالاستخدام!** 😊

---

## 📞 الدعم

إذا واجهت مشاكل في النشر:
1. راجع السجلات في Render
2. تحقق من الإعدادات
3. افتح Issue في GitHub
